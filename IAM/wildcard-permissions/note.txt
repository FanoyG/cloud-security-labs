Local-only step (not committed)

For demonstration, an access key was created locally:

```hcl
resource "aws_iam_access_key" "test_key" {
  user = aws_iam_user.test_user.name
}


This block is intentionally excluded from version add it in iam_user.tf otherwise you will face error.